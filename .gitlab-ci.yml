image: python:3.7-alpine

pages:
  stage: deploy
  script:
  - python3 -V
  - pip3 install -r requirements.txt
  - cd docs
  - make html
  - mv build/html/ ../public/
  artifacts:
    paths:
    - public
  only:
  - master
