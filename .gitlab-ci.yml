image: python:3.7-alpine

pages:
  stage: deploy
  script:
  - python3 -V
  - cd docs
  - pip3 install -r requirements.txt
  - make html
  - mv build/html/ ../public/
  artifacts:
    paths:
    - public
  only:
  - master
